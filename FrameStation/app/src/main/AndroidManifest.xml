<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Forward server-triggered gamepad rumble to the headset via the Vibrator API. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.software.xr.api.spatial" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.FrameStation"
        tools:targetApi="35">

        <property
            android:name="android.window.PROPERTY_XR_ACTIVITY_START_MODE"
            android:value="XR_ACTIVITY_START_MODE_FULL_SPACE_MANAGED" />

        <!-- Stream service processes — each runs in its own isolated process for
             libmoonlight-core.so separation. Three simultaneous streams = three processes.
             Distinct class names required: Android rejects duplicate android:name entries. -->
        <service
            android:name=".streaming.StreamService0"
            android:process=":stream0"
            android:exported="false" />

        <service
            android:name=".streaming.StreamService1"
            android:process=":stream1"
            android:exported="false" />

        <service
            android:name=".streaming.StreamService2"
            android:process=":stream2"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.FrameStation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
